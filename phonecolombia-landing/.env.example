# Plantillas de entorno:
#   env.local       → copy env.local .env        (desarrollo)
#   env.production  → copy env.production .env   (build para Hostinger)
#
# Backend (carpeta backend/):
#   env.local       → copy env.local .env
#   env.production  → copy env.production .env   (en el servidor)

VITE_API_URL=http://localhost:8000/api
